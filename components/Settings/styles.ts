import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        margin: 20,
    },
    inputContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 10,
        padding: 8,
    },
    urlInput: {
        flex: 2,
        marginRight: 8,
    },
    separator: {
        marginHorizontal: 4,
    },
    portInput: {
        flex: 1,
        marginLeft: 8,
    },
});
